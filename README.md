unzip folder, ensure node.js is installed on machine and expo go is installed on mobile device:

if not in formbase directory, run cd formbase

npm install 

npx expo start 

scan qr code from mobile device
