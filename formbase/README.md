unzip folder, ensure node.js is installed on machine and expo go is installed on mobile device:

1. if not in formbase directory, run ```cd formbase```

2. ```npm install ```
3. ```npx expo start ```
4. scan qr code from mobile device 

